/**
 * French UI labels for the week selector component.
 */

export const WEEK_SELECTOR_LABELS = {
  label: "Semaine",
  currentWeek: "Menu de la semaine",
} as const;

/** Unicode indicator for finalized periods */
export const FINALIZED_INDICATOR = " ✓";
